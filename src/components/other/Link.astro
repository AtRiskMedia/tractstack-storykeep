---
export interface Props {
  href: string;
  className?: string;
  ariaLabel?: string;
  title?: string;
  disabled?: boolean;
  target?: string;
}

const {
  href,
  className,
  ariaLabel,
  title,
  disabled = false,
  target = "_self",
} = Astro.props;
---

{
  disabled ? (
    <span
      class={`group inline-block ${className}`}
      aria-label={ariaLabel}
      title={title}
      aria-disabled={disabled}
    >
      <slot />
    </span>
  ) : (
    <a
      {href}
      class={`group inline-block hover:text-myblue ${className}`}
      aria-label={ariaLabel}
      title={title}
      target={target}
    >
      <slot />
    </a>
  )
}
