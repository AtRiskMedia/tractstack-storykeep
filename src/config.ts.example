import type { Site } from "./types";

export const SITE: Site = {
  website: "https://tractstack.com/", // replace this with your domain
  author: "At Risk Media",
  desc: "the website builder that converts",
  title: "TractStack",
  ogImage: "og.png",
  ogLogo: "logo.png",
};

export const LOCALE = {
  lang: "en",
  langTag: ["en-EN"],
} as const;
